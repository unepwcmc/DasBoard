<%= form_for objective, remote: true, :html => {class: 'objective-metric-form'} do |f| %>
  <%= f.select :metric_id, options_from_collection_for_select(@metrics, :id, :name) %>
  <%= f.submit %>
<% end %>
<%= link_to "Create metric", new_metric_path %>
